export type LoginType = {
  username: string;
  password: string;
};

export type RegisterType = {
  username: string;
  email: string;
  password: string;
};

export type GoogleUserInfo = {
  email: string;
  name: string;
  picture: string;
  sub: string;
};
